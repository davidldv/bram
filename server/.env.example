OPENROUTER_API_KEY=sk-or-v1-xxxxx
OPENROUTER_BACKUP_KEY=
OPENROUTER_BACKUP_KEY2=
NEWS_API_KEY=xxxxx
OPENROUTER_MODEL=openai/gpt-oss-120b:free
PORT=3000
RATE_LIMIT_MAX=30
# Shared secret the app sends as the x-bram-key header. If unset, endpoints are
# open to anyone with the URL. Set the same value in app.json > expo.extra.clientSecret.
CLIENT_SECRET=
